# CHALK 2.0 Planning Mirror

This folder is the repository copy of the CHALK 2.0 planning and audit artifacts that were originally drafted in the local `.chalk/` workspace.

Canonical branch: `feature/chalk-2.0-renovation`

Canonical remote for this work: `fork` / `git@github.com:ismaeldosil/chalk-coaching-app.git`

Client upstream remote: `origin` / `https://github.com/chalkclassroom/CoachingApp` is fetch-only locally. Do not push V2 code or documentation to CHALK until budget approval and explicit instruction.

Start with `CHALK-2-INDEX.md`, then read `CHALK-2-ARCHITECTURE-DOC-PLAN.md` for the architecture-documentation execution plan.
